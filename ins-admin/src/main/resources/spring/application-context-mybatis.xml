<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">

	<beans profile="dev">
		<!-- myBatis文件 -->
		<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
			<property name="dataSource" ref="dataSource" />
			<property name="mapperLocations" value="classpath*:mybatis/*Mapper.xml" />
			<property name="configLocation" value="classpath:spring/mybatis-config.xml" />
		</bean>
	</beans>

	<!-- Jenkins CI是使用dev profile，但也是使用SpringBoot uber Jar，所以必须用SpringBootVFS，所以要把ci 
		profile加上， 那么只能把这个bean声明放后面，在同时有ci和dev时覆盖dev的bean -->
	<!--<beans profile="!dev,ci">
		&lt;!&ndash; myBatis文件 &ndash;&gt;
		<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
			<property name="dataSource" ref="dataSource" />
			<property name="mapperLocations" value="classpath*:mybatis/*Mapper.xml" />
			<property name="configLocation" value="classpath:spring/mybatis-config.xml" />
			<property name="vfs">
				<bean class="java.lang.Class" factory-method="forName">
					<constructor-arg
						value="cn.com.chengyisheng.mybatis.springboot.SpringBootVFS" />
				</bean>
			</property>
		</bean>
	</beans>-->

	<beans>
		<bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
			<property name="basePackage"
				value="cn.com.yikangbao.dao" />
			<property name="sqlSessionFactoryBeanName" value="sqlSessionFactory" />
			<property name="annotationClass" value="org.springframework.stereotype.Component"/>
		</bean>
	</beans>
</beans>
