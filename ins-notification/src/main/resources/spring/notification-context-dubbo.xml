<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:beans="http://www.springframework.org/schema/beans"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://www.springframework.org/schema/beans"
	   xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
	   xsi:schemaLocation="
	http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://code.alibabatech.com/schema/dubbo
    http://code.alibabatech.com/schema/dubbo/dubbo.xsd">


	<!-- 提供方应用信息，用于计算依赖关系 -->
	<dubbo:application name="${dubbo.application.name}" />
	<!-- 用dubbo协议暴露服务 -->
	<dubbo:protocol name="dubbo" port="${dubbo.protocol.port}" accesslog="${service.accesslog.location}" />

	<!-- zookeeper地址 -->
	<dubbo:registry address="${dubbo.registry.address}" />
	<dubbo:monitor protocol="${dubbo.monitor.protocol}"  />


	<!-- 声明需要暴露的服务接口 -->
	<dubbo:service
			interface="cn.com.yikangbao.service.notification.WechatNotificationService"
			ref="wechatNotificationServiceRPC" timeout="3000"/>
	<bean id="wechatNotificationServiceRPC"
		  class="cn.com.yikangbao.service.notification.impl.WechatNotificationServiceImpl" />
</beans>
